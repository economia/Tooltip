# Tooltip plugin

Převelice jednoduchý jQuery plugin, hlídající mouseover nad elementy s atributem data-tooltip.

### Instalace:

    npm install -g LiveScript
    npm install uglify-js stylus
    slake build

### Použití
    new Tooltip().watchElements();

### Licence

MIT
